# Done to increase the memory available to gradle.
org.gradle.jvmargs=-Xmx1G
org.gradle.parallel=true
# Let Gradle use the JDK from JAVA_HOME (or the daemon's JVM) so the build works
# on any machine. Set org.gradle.java.home locally if you need a specific JDK.

# IntelliJ IDEA is not yet fully compatible with configuration cache, see: https://github.com/FabricMC/fabric-loom/issues/1349
org.gradle.configuration-cache=false

# Fabric Properties
# check these on https://fabricmc.net/develop
minecraft_version=1.21.11
yarn_mappings=1.21.11+build.6
loader_version=0.19.2
loom_version=1.16.3

# Mod Properties
mod_version=1.0.0
maven_group=com.food.giamat

# Dependencies
fabric_api_version=0.139.5+1.21.11
